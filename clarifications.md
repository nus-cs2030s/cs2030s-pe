
<div style="font-size: 200%;">
<h2>Question 2</h2>

<p><strong>Correction 1:</strong><br>
The <code>toString()</code> method should return the string representation of a request.
The format of the string should represent the line segment as:
<code>(x1, y1) -&gt; (x2, y2)</code>.
</p>

<p><strong>Correction 2:</strong><br>
The <code>Request</code> class has a constructor that accepts four <code>int</code> values:
<code>x1</code>, <code>y1</code>, <code>x2</code>, and <code>y2</code>.
</p>

<p>
The coordinate <code>(x1, y1)</code> represents the origin, and
<code>(x2, y2)</code> represents the destination.
</p>
</div>
